# Logging

- 日志不是独立分层；只在命中层的必要路径上调整。
- 复用项目统一日志库、上下文字段和结构化格式，不引入新的日志接口。
- 失败日志应包含函数或操作名、关键业务标识和下游错误；避免记录令牌、密钥和完整敏感载荷。
- 按项目语义选择 Error、Warn、Info、Debug，避免同一错误在每层重复记录。
- 成功日志只保留关键业务节点，不把循环内逐条处理默认记录为 Info。
- 日志不能代替错误传播；除非任务框架有明确约定，不要记录错误后返回成功。
