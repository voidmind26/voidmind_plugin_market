# Service

- `service` 负责业务规则、业务编排、错误处理、响应组装和业务日志。
- 方法签名、上下文类型、依赖获取方式与同模块现有实现保持一致。
- 通过 data/cache 层访问持久化与缓存，不直接拼 SQL 或下沉复杂 Redis 命令。
- 避免逐条查询导致 N+1；优先使用项目已有批量接口。
- 不为当前需求切换单例、构造器、依赖注入或目录组织方式。
- 失败路径保留足够上下文并按现有策略返回或包装错误，避免日志后返回 `nil` 掩盖失败。
- 响应映射只做契约转换；不要在映射循环中隐式增加额外查询或无关业务规则。
